#pragma once
#include "defines.hpp"
// @breif 	routine to abort an application without clean up
void habort(void);

// @breif 	routine to abort an application without clean up
// @param	: the return exit code
void hexit(i32 exit_code);
